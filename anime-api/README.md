# Anime API

Project documentation is maintained in the repository root README.

- Main docs: ../README.md
- Local commands in this folder still apply (for example `npm run build`, `npm test`, `npm run dev`).